{
    "name": "element-book",
    "version": "0.0.0",
    "homepage": "https://github.com/electrovir/element-book",
    "bugs": {
        "url": "https://github.com/electrovir/element-book/issues"
    },
    "repository": {
        "type": "git",
        "url": "https://github.com/electrovir/element-book"
    },
    "license": "MIT",
    "author": {
        "name": "electrovir",
        "url": "https://github.com/electrovir"
    },
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "workspaces": [
        "./packages/*"
    ],
    "scripts": {
        "compile": "rm -rf packages/*/node_modules/@element-book && rm -rf node_modules/@element-book && npm i && rm -rf packages/*/dist && rm -f packages/*/tsconfig.tsbuildinfo && tsc --build tsconfig.build.json && npm run compile --workspaces --if-present",
        "docs:update": "virmator code-in-markdown",
        "format": "virmator format",
        "publish": "virmator publish \"npm run compile && npm run test:all\"",
        "start": "npm run start --workspace=@element-book/frontend-example",
        "test": "tsc --build tsconfig.build.json && npm run test --workspaces -s --if-present",
        "test:all": "npm run compile && npm run test:spelling && npm run test:format && npm run test:docs && npm run test:coverage",
        "test:coverage": "npm run test:coverage --workspaces -s --if-present",
        "test:docs": "virmator code-in-markdown check",
        "test:format": "virmator format check",
        "test:spelling": "virmator spellcheck",
        "test:types": "tsc --build tsconfig.build.json"
    },
    "devDependencies": {
        "cspell": "^6.31.1",
        "markdown-code-example-inserter": "^0.3.0",
        "prettier": "^2.8.7",
        "prettier-plugin-interpolated-html-tags": "^0.0.3",
        "prettier-plugin-jsdoc": "^0.4.2",
        "prettier-plugin-multiline-arrays": "^1.1.3",
        "prettier-plugin-organize-imports": "^3.2.2",
        "prettier-plugin-packagejson": "^2.4.3",
        "prettier-plugin-sort-json": "^1.0.0",
        "prettier-plugin-toml": "^0.3.1",
        "virmator": "^6.2.2"
    }
}
